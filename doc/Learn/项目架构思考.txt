一个完整的项目
底层的数据库，
连接数据库的代码层
功能的代码，
实体类，
作为基本盘的接口，
连接前端的api，
UI界面


实体往往对应数据库的一个表
项目完成后，就是用户使用UI界面，产生交互，调用api，api找对应的接口，
接口连接功能模块，new实体类进行运算，然后CRUD，然后再层层返回


实际工作的新发现：
1，项目运行还需要各种各样的配置文件
UI内部画面控件与api绑定的配置
java项目里外来库的版本配置
连接数据库所使用的sql.xml


2，安全检定
层层验证用户身份，传输时不直接显示用户信息，用户权限控制
